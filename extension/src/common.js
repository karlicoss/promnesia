/* @flow */

export class Visit {
    // TODO parse it
}

export class Visits {
    visits: Array<any>;
    contexts: Array<any>;

    constructor(visits: Array<any>, contexts: Array<any>) {
        this.visits = visits;
        this.contexts = contexts;
    }
}
