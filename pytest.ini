[pytest]
# discover __init__,py as well
python_files = *.py
norecursedirs = tests/testdata/*
addopts =
  --verbose

  # otherwise it won't discover doctests
  --doctest-modules
testpaths = tests
markers =
    with_browser: marks tests as needing a browser to run
