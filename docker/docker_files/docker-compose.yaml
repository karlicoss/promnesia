version: '3.3'

services:
  server:
    image: promnesia
    build:
      context: ../
      dockerfile: docker/Dockerfile
    # Uncomment to enable persisent volumes
    volumes:
      - ./data:/data
    ports:
      - "13131:13131"
    restart: always
  indexer:
    depends_on:
      - server
    image: promnesia-indexer
    build:
      context: ../
      dockerfile: docker/Dockerfile-indexer
    # Uncomment to enable persisent volumes
    # create indexer-config.py inside ./promnesia-data
    volumes:
      - ./data:/data
    environment:
      # run once every ten minutes
      CRONTAB: "0-59/10 * * * * cd /usr/src/promnesia &&  /usr/local/bin/python -m promnesia index  --config /data/indexer-config.py"
